# EDAV Private Endpoint Monitor - Python Dependencies
# Install with: pip install -r requirements.txt

# Azure SDK (CLI wrapper - az CLI must be installed separately)
# Download Azure CLI: https://aka.ms/installazurecliwindows

# Data processing
pandas>=2.0.0
openpyxl>=3.1.0

# Excel styling (included with openpyxl)
# No extra packages needed for email (uses Python stdlib smtplib)
